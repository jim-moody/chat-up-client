<div class="form-card card" data-id="{{line.id}}">
<div class="line-text-container">
  <div class="line-text">
    <p>
    {{#if line.text}}
    {{line.text}}
    {{else}}
    No description.
    {{/if}}
    </p>

  </div>
 {{#if line.editable}}
 <div class="edit-link-container"><i class="edit-options fa fa-ellipsis-v fa-lg" aria-hidden="true"></i>
   <!-- Dropdown Structure -->
   <ul  style="display:none;" class='card edit-dropdown-content'>
     <li class="edit" data-id="{{line.id}}">Edit</li>
     <li class="delete" data-id="{{line.id}}">Delete</li>
   </ul>
 </div>
 {{/if}}
</div>
<div id="votes-{{line.id}}" class="voting-container">
  <div class="vote-container">
    <span data-id="up-vote-{{line.id}}">{{line.voteSummary.up}}</span>
    <i data-value="1" data-id="{{line.id}}"class="vote up-vote material-icons">thumb_up</i>
  </div>
    <div data-id="total-points-{{line.id}}" class="total vote-container">{{line.voteSummary.total}}</div>
  <div class="vote-container">
    <span data-id="down-vote-{{line.id}}">{{line.voteSummary.down}}</span>
    <i data-value="-1" data-id="{{line.id}}" class="vote down-vote material-icons">thumb_down</i>
  </div>
</div>
</div>
